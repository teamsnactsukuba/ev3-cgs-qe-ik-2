// IKP_Trajectory_QE.rr
// A program for computing path and trajectory planning based on CGS-QE method
// Mizuki Yoshizawa and Akira Terui
// Team SNAC Tsukuba, 2023

load("IKP_Point.rr")$

// Trajectory using CGS-QE (not completed)
def ikp_trajectory_qe(CGS, Var, Par, Ord){
  CGSSize = length(CGS);
  RealCGS = generate_realcgs(CGS, Para, Jun);
  RealCGSSize = length(RealCGS);
  return RealCGS;
}

end$